Thử thách như sau hiện ra với dòng chữ `You are using Firefor`, có lẽ nó liên quan đến `User-agent`

![img1]()

Tôi dùng burpsuite với `Chrome` thì nhận được `You are using Chrome`

![img2]()

Vậy bài này cần tìm đúng `User-agent` để lấy `flag`, tôi nhìn tên đề bài thì đoán ra đáp án là `Googlebot`

![image3]()

[ Google crawlers (user agent)](https://developers.google.com/search/docs/crawling-indexing/overview-google-crawlers?hl=en&visit_id=638583964640295323-1218094715&rd=1)

![img4]()

> Flag:
